{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.8.4"
  },
  "scripts": {
    "lint": "lerna run lint",
    "test": "lerna run test",
    "install": "npm run bootstrap",
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build"
  }
}
