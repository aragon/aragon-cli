import { AragonConfig } from './types'

const defaultAragonConfig: AragonConfig = {
  appServePort: 8001,
  clientServePort: 3000,
  appSrcPath: 'app/',
  appBuildOutputPath: 'dist/'
}

export default defaultAragonConfig
