const manifestMiddleware = require('./manifest')
const moduleMiddleware = require('./module')
const environmentMiddleware = require('./environment')

module.exports = {
  manifestMiddleware,
  moduleMiddleware,
  environmentMiddleware
}
